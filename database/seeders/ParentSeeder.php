<?php

namespace Database\Seeders;

use Illuminate\Database\Seeder;

/**
 * ParentSeeder — intentionally disabled for production.
 *
 * Parent accounts are created through the admin UI, never seeded.
 */
class ParentSeeder extends Seeder
{
    public function run(): void
    {
        // No-op. Disabled in production.
    }
}
