<?php

namespace Database\Seeders;

use Illuminate\Database\Seeder;

/**
 * DemoDataSeeder — intentionally disabled for production.
 *
 * All tenant data (students, teachers, classes, subjects, etc.)
 * is managed through the admin UI, never seeded.
 */
class DemoDataSeeder extends Seeder
{
    public function run(): void
    {
        // No-op. Demo seeding is disabled in production.
    }
}
